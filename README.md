# CNN Shape Recognizer 🟦 🟢

Десктопное приложение на Python для генерации датасета, обучения сверточной нейронной сети (CNN) и распознавания геометрических фигур (окружностей и квадратов).

Проект реализован с использованием архитектурного паттерна **MVC (Model-View-Controller)** и библиотеки **Tkinter** для графического интерфейса.

## 📋 Функционал

1.  **Генерация датасета**:
    * Автоматическое создание изображений геометрических фигур (квадраты и окружности).
    * Настройка количества изображений.
    * Сохранение в структурированные папки.
2.  **Обучение модели (CNN)**:
    * Создание архитектуры сверточной нейронной сети с помощью **TensorFlow/Keras**.
    * Обучение на сгенерированных данных.
    * Визуализация прогресса обучения (Accuracy/Loss) в реальном времени.
3.  **Тестирование**:
    * Загрузка пользовательских изображений (JPG, PNG).
    * Классификация изображения (Окружность или Квадрат).
    * Визуальное отображение результата с цветовой индикацией.

## 🛠 Технологии

* **Python 3.10+**
* **TensorFlow / Keras** — построение и обучение нейросети.
* **Tkinter** — графический интерфейс (GUI).
* **Pillow (PIL)** — работа с изображениями.
* **NumPy** — работа с массивами данных.

## 📂 Структура проекта

```text
├── controllers/       # Логика управления (связь Model и View)
├── models/            # Логика данных (генератор, нейросеть)
├── views/             # Графический интерфейс (окна, виджеты)
├── data/              # Папка для сгенерированного датасета (создается автоматически)
├── requirements.txt   # Список зависимостей
├── main.py            # Точка входа в приложение
└── README.md          # Документация

```

## 🚀 Установка и запуск

Для запуска проекта необходимо установленное виртуальное окружение и зависимости.

### 1. Клонирование репозитория

```bash
git clone https://github.com/tosqaa/cnn-shape-recognizer.git
cd cnn-shape-recognizer

```

### 2. Создание виртуального окружения (venv)

**Windows:**

```bash
python -m venv venv
venv\Scripts\activate

```

**macOS / Linux:**

```bash
python3 -m venv venv
source venv/bin/activate

```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt

```

### 4. Запуск приложения

```bash
python main.py

```

## 📖 Руководство по использованию

1. **Шаг 1: Генерация.**
В блоке "Генерация датасета" укажите количество изображений (например, `200` на каждый класс) и нажмите **"Сгенерировать"**. Дождитесь заполнения шкалы прогресса.
2. **Шаг 2: Обучение.**
В блоке "Обучение Модели" нажмите **"Обучить Нейросеть"**. Приложение обучит модель на 5 эпохах. Следите за ростом точности (`Acc`).
3. **Шаг 3: Тестирование.**
Нажмите **"Загрузить и Проверить"**, выберите любое изображение с геометрической фигурой. Программа выведет результат и подсветит его цветом (Зеленый — Квадрат, Синий — Окружность).

## 📝 Заметки

* Для тестирования лучше использовать изображения с темной фигурой на светлом фоне (так как сеть обучалась на таких данных).
* Модель сохраняется в папку `models/` в формате `.keras`.
